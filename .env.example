# FastAPI / Docker environment
DEBUG=False
SECRET_KEY=change-me-in-production-stock-predictive

# Production UI port (docker-compose.yml → nginx)
APP_PORT=8080
# Development UI port (docker-compose.dev.yml → Vite HMR). Keep this 5173.
DEV_UI_PORT=5173
API_PORT=8000

CORS_ORIGINS=http://localhost:8080,http://localhost:5173

POSTGRES_DB=stock_predictive
POSTGRES_USER=stock
POSTGRES_PASSWORD=stockpass
